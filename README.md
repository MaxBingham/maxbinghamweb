# maxbinghamweb

Personal site (Eleventy + Simple.css).

## Run locally

```bash
npm install
npm start
```

## Deploy

GitHub Pages via Actions on push to `main`. Site URL is set in `_data/metadata.js`.
